# Elixir + wxWidgets テトリスアプリ計画

## プロジェクト名
`elixir_wx_tetris`

## 運用ルール
- コミットメッセージは必ず日本語で記述する
- 必ず新規でプロジェクトを作成してから開発を開始する

## 開始前チェックリスト
- 作業ディレクトリ直下に既存 `mix.exs` がないことを確認する
- 新規作成先は `/mnt/4tb/user/local/wx_widgets/elixir_wx_tetris` とする
- 既存ディレクトリ名が衝突した場合は作成を中止し、別名で再作成する
- `mix new elixir_wx_tetris` 実行後、初回コミット前に `mix test` が通ることを確認する

## 目的
Elixir で `:wx` を使ったテトリスを作成する

## 開発方針
- まずは「遊べる最小構成」を作り、その後に機能追加する
- 描画とゲームロジックを分離し、保守しやすくする
- 1機能ごとに小さくコミットする

## フェーズ計画
### Phase 1: プロジェクト骨組み
- `mix new elixir_wx_tetris` でプロジェクト作成
- `Tetris.Application` で wx 起動処理を定義
- `Tetris.Window` でメインウィンドウ生成
- `Tetris.GameLoop`（GenServer）でゲーム状態更新の土台を作る

### Phase 2: 画面描画（固定盤面）
- 盤面サイズを定義（例: 幅10 x 高さ20）
- `wxPanel` 上にグリッドを描画
- セル単位で色付き矩形を描画するヘルパーを作成
- まずは固定ブロック配置を描画して表示確認

### Phase 2.5: 入力層先行実装
- `Tetris.Input` を作成し、キーコード -> アクション契約を先に固定する
- `:wx` の `:key_down` イベントを採用し、`unicode_key` / `key_code` で解釈する
- JIS/US 差を吸収するため、文字キーは大文字小文字を正規化して判定する

### Phase 3: テトリミノ実装
- 7種ミノ（I/O/T/S/Z/J/L）の座標定義
- スポーン位置と回転状態を管理
- 現在ミノの描画を追加
- 左右移動・回転・ソフトドロップ入力を実装

### Phase 4: ゲームルール実装
- 一定間隔で自動落下（タイマー駆動）
- 着地判定と盤面固定
- ライン消去とスコア加算
- 次ミノ生成（7-bag方式）
- ゲームオーバー判定
- 更新順序を固定（入力適用 -> 重力落下 -> 固定判定 -> ライン消去）

### Phase 5: UI改善
- スコア表示
- 次ミノ表示（Next）
- 一時停止（Pause）と再開
- リスタート機能

### Phase 6: 品質向上
- 盤面ロジック（衝突判定、ライン消去）をユニットテスト
- 入力連打時の安定性確認
- 速度段階（レベル）調整
- コード整理と README 作成

## モジュール案
- `Tetris.Application`: アプリ起動
- `Tetris.Window`: wx フレームとイベント配線
- `Tetris.Input`: キーイベント -> アクション変換
- `Tetris.Renderer`: 描画処理
- `Tetris.Board`: 盤面データとライン処理
- `Tetris.Piece`: ミノ定義と回転処理
- `Tetris.GameLoop`: 状態更新、タイマー、入力反映

## マイルストーン
1. 盤面と固定ブロックが表示される
2. ミノを操作して落下できる
3. ライン消去とスコアが動く
4. 1ゲーム通して遊べる

## 完了条件
- キーボード操作でテトリスがプレイ可能
- クラッシュせず連続プレイできる
- 最低限のテスト（盤面ロジック）が通る
- キー操作テスト（入力変換・状態変化・連続入力）が全通過する

## キー操作仕様
- `←` : ミノを左移動
- `→` : ミノを右移動
- `↓` : ソフトドロップ（押下中は落下速度を上げる）
- `↑` または `X` : 右回転
- `Z` : 左回転
- `Space` : ハードドロップ（一気に落下して固定）
- `P` : 一時停止 / 再開
- `R` : ゲームリスタート
- `Esc` : 終了
- 文字キーは `x/z/p/r` の大文字小文字を同一扱いにする（`X` と `x` は同じ）

## 入力処理メモ
- `wxKeyEvent` の `:key_down` を `Tetris.Window` で受け取り、`Tetris.Input` でアクションに変換して `Tetris.GameLoop` に送る
- 押しっぱなし対応が必要なキー（`←`,`→`,`↓`）はキーリピートを考慮する
- 回転・ハードドロップは単発入力として処理する
- `Esc` / `Space` / 矢印キーは `key_code` ベースで判定する
- `x/z/p/r` は `unicode_key` を小文字化して判定する

## キー操作テスト計画
### 目的
- キー入力が正しいアクションに変換されること
- アクション適用後のゲーム状態が期待どおりに変化すること
- 連続入力・境界条件でも誤動作しないこと

### テスト対象分離
- `Tetris.Input`（新規想定）: キーコード -> アクション変換
- `Tetris.GameLoop` / `Tetris.Board`: アクション適用結果の検証

### 単体テスト（Input変換）
- `←` 入力で `:move_left`
- `→` 入力で `:move_right`
- `↓` 入力で `:soft_drop`
- `↑`/`X` 入力で `:rotate_cw`
- `Z` 入力で `:rotate_ccw`
- `Space` 入力で `:hard_drop`
- `P` 入力で `:toggle_pause`
- `R` 入力で `:restart`
- `Esc` 入力で `:quit`
- `x/X`, `z/Z`, `p/P`, `r/R` の大文字小文字差を吸収できる
- 未定義キー入力で `:noop`（無視）

### 単体テスト（状態変化）
- 左端で `:move_left` しても盤面外に出ない
- 右端で `:move_right` しても盤面外に出ない
- 衝突時に回転不可なら状態を維持する
- `:soft_drop` で1段だけ落下する
- `:hard_drop` で着地位置まで落下して固定される
- `:toggle_pause` 中は落下タイマー更新を停止する
- `:restart` でスコア・盤面・現在ミノが初期化される

### 連続入力テスト
- `←` 長押し相当で複数回移動しても壁を超えない
- `→` と回転の高速交互入力でもクラッシュしない
- 接地直前の `:hard_drop` 連打でも二重固定しない
- `pause` 切替直前/直後の入力で状態不整合が起きない
- 同一 tick で「回転 + 接地」が発生しても更新順序どおりに処理される

### 実装メモ（ExUnit）
- キー変換は純関数化して `test/tetris/input_test.exs` で検証
- ゲーム状態更新も可能な範囲で純関数化し、`test/tetris/game_loop_test.exs` で検証
- タイマー依存箇所は `tick` メッセージを直接送って疑似的に再現
- ミノ供給はDI可能にし、テスト時は固定シーケンスで再現性を担保する
