# Elixir + wxWidgets ミニアプリ3D計画

## プロジェクト名
`elixir_3d_gallery`


## 目的
Elixir で `:wx`（Erlang/OTP 同梱の wxWidgets バインディング）を使い、3Dを表示する。  
OpenGLで描画し、複数種類のオブジェクトを切り替えて表示する。

## アイデア出し

### 1. オブジェクト表示アイデア
- 基本プリミティブ: 立方体 / 球 / 円柱 / 円錐 / トーラス
- 拡張形状: ピラミッド / カプセル / グリッド地形
- 複合オブジェクト: ロボットアーム（複数パーツ） / ミニ太陽系（球体+軌道）
- 外部データ読み込み: `OBJ` 形式メッシュ（将来的に）

### 2. 表示モード案
- 単体表示モード: 1つのオブジェクトを大きく表示
- ギャラリーモード: 複数オブジェクトを並べて回転表示
- シーンモード: 床 + ライト + 複数オブジェクト配置
- ワイヤーフレーム切替: `fill / line` 切替

### 3. 操作アイデア
- マウスドラッグ: カメラ回転（Orbit）
- マウスホイール: ズーム
- 右ドラッグ or キー: 平行移動
- キー入力:
  - `1..9`: オブジェクト種別切替
  - `R`: 回転リセット
  - `L`: ライトON/OFF
  - `W`: ワイヤーフレーム切替
  - `A`: 自動回転ON/OFF

### 4. 見た目強化アイデア
- ライティング: 環境光 + 拡散光 + 鏡面反射
- マテリアル: 金属/プラスチック風の質感プリセット
- 背景: グラデーション背景色
- シャドウ（後段）: 簡易影 or 平面投影影

### 5. 実装ステップ（最小から拡張）
1. `:wx` でウィンドウとOpenGLコンテキスト作成
2. 三角形/立方体の描画（描画ループ確認）
3. カメラ行列（投影/ビュー）導入
4. 複数オブジェクト切替機能を追加
5. 入力操作（マウス・キー）を追加
6. ライト・マテリアルを追加
7. シーンモード化（複数配置）
8. 外部モデル読み込み（OBJ）検討

### 6. 技術メモ（Elixir + :wx）
- OpenGL描画は `:wxGLCanvas` + `:wxGLContext` を利用
- 再描画は `:paint` イベントで処理し、必要時に `:wxWindow.refresh/1`
- アニメーションは `:wxTimer` で周期更新
- 画面サイズ変更時にビューポート再設定（`glViewport`）

### 7. 最初のMVP定義
- 立方体・球体・円柱の3種類を表示可能
- マウスで回転、ホイールでズーム
- `1/2/3` キーでオブジェクト切替
- 自動回転トグルあり

---

## 次に決めること（確定）

### A. 描画方式
- **固定機能パイプラインで開始する（確定）**
- 目的: MVPを最短で成立させる
- 将来: MVP後にシェーダーベースへ段階移行を検討

### B. MVP完成条件
- 立方体 / 球体 / 円柱の切替ができる
- マウス回転・ホイールズームが自然に動く
- 自動回転ON/OFFができる
- 操作中に滑らかに動作する（目標60fps、最低30fps）

### C. 最初のサンプルシーン
- **単体表示モードで開始する（確定）**
- 理由: カメラ・入力・描画の基礎検証に集中できる

---

## 実装チェックリスト（次アクション）
- [ ] `:wxApp` と `:wxFrame` の起動コードを作る
- [ ] `:wxGLCanvas` / `:wxGLContext` を初期化する
- [ ] `paint` イベントで立方体を描画する
- [ ] `:wxTimer` で再描画ループを作る
- [ ] `1/2/3` キーで形状切替を実装する
- [ ] マウスドラッグ回転とホイールズームを実装する
- [ ] `A` キーで自動回転ON/OFFを実装する
- [ ] FPSを簡易計測して30fps以上を確認する
