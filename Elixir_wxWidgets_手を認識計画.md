# Elixir + wxWidgets カメラで手を認識プリ計画書

## プロジェクト概要
- プロジェクト名: `elixir_wx_hand_pose_viewer`
- 目的: Elixir で `:wx` を使ったカメラアプリを作成する
- ゴール
  - USB カメラの映像をそのまま表示する
  - 手を認識したら関節を認識して緑の線を引く

## 技術方針（先に確定する事項）
- Python は使用しない（MediaPipe 直接利用は行わない）
- カメラ取得方式は `Evision`（OpenCV バインディング）を第一候補とする
- 代替案として V4L2 直接利用は残すが、初版では採用しない
- フレーム取得（カメラI/O）と GUI 描画（`:wx`）は別プロセスに分離する
- フレーム受け渡しは「最新1フレームのみ保持」を原則とし、古いフレームは破棄する
- フレーム形式は RGB に正規化してから描画する
- 対応OSは Linux を優先し、`/dev/video*` の利用を前提にする

## 前提環境（Linux）
- OpenCV を有効化した `Evision` がビルド可能であること
- カメラ利用ユーザーが `video` グループ等の必要権限を持つこと
- `Evision.VideoCapture` の優先バックエンドは V4L2、失敗時は環境依存の代替バックエンドにフォールバックすること
- 起動時に使用デバイス、解像度、FPS、利用バックエンドをログ出力すること

## 参照実装
- 初期実装は `./elixir_wx_camera_viewer` を参照し、以下を踏襲する
  - プロセス構成（カメラI/OとGUI描画の分離）
  - 描画ループ（RGB正規化後に `:wx` へ反映）
  - 終了処理（カメラ解放後にGUI停止）
- まずは「カメラ表示基盤（手認識なし）」を完成させてから手認識機能を追加する

## 運用ルール
- コミットメッセージは必ず日本語で記述する
- 必ず新規でプロジェクトを作成してから開発を開始する
- 新規プロジェクト作成は必須としつつ、`./elixir_wx_camera_viewer` からのコード移植は可とする（移植元を記録する）
- 作業開始前に必ず [共通運用ルール](共通運用ルール.md) を確認する

## 開始前チェックリスト
- 作業ディレクトリ直下に既存 `mix.exs` がないことを確認する
- 新規作成先は `./elixir_wx_hand_pose_viewer` とする
- 既存ディレクトリ名が衝突した場合は作成を中止し、別名で再作成する
- `mix new elixir_wx_hand_pose_viewer` 実行後、初回コミット前に `mix test` が通ることを確認する
- カメラデバイス（例: `/dev/video0`）が認識され、権限不足がないことを確認する

## 実行プラン
0. 手認識方式の事前選定（Phase 0）
- 採用方式（決定）: OpenCV DNN（`Evision.dnn`）+ ONNX手キーポイントモデル
- 採用モデル情報（固定）
  - モデル名: Hand Keypoint 21 (ONNX)
  - バージョン: v1
  - 配布元URL: `https://github.com/open-mmlab/mmpose`
  - ライセンス: Apache-2.0（商用利用可、再配布可）
- 構成: 学習済みONNXモデルを `Evision.dnn` で読み込み、フレームごとに推論して関節座標を取得する
- 入出力仕様（固定）
  - 入力テンソル shape: `1x3x224x224`
  - 前処理: BGR入力をRGBへ変換、`[0,1]` 正規化、letterboxリサイズ
  - 出力テンソル shape: `1x21x3`（`x, y, score`）
  - 信頼度しきい値: `0.35`
  - ランドマーク点数: `21`
  - 接続定義: `0-1-2-3-4, 0-5-6-7-8, 0-9-10-11-12, 0-13-14-15-16, 0-17-18-19-20`
- 利点: Python不要でElixir側に統合しやすく、配布構成を単純化しやすい
- リスク: モデル選定と前後処理の実装難易度が高く、精度・速度の調整が必要
- 向いている条件: 単一バイナリ寄りの運用を重視し、OpenCVベースで完結したい場合
- 代替方式（Aが不達の場合のみ）: TFLite C API を NIF/Port 経由で呼び出す方式
- PoC完了条件: 1手以上の関節点を取得し、GUIに重畳できること
- ライセンス条件: 商用利用を含めて再配布可能で、利用条件が明確なモデルのみ採用する
- 終了条件: GUIのウィンドウクローズ操作、またはESCキー入力で必ず正常終了できること
- 静止画1枚で推論結果が再現できる最小検証手順を作成する
- 許容性能目標: 640x480 で 15 FPS 以上、表示遅延 200ms 未満
- 性能測定条件を固定する
  - 計測条件: 推論有効、640x480、10分連続実行
  - FPS定義: 直近5秒の描画完了数から算出した平均FPS
  - 遅延定義: フレーム取得時刻から画面反映時刻までのP50/P95
  - 合格基準: 平均FPS 15以上、P95遅延 200ms 未満
  - 参考指標: CPU使用率・メモリ使用量を記録

1. 事前確認
- [共通運用ルール](共通運用ルール.md) を確認する
- 作業ディレクトリと命名衝突有無を確認する

2. プロジェクト作成
- `mix new elixir_wx_hand_pose_viewer` を実行する
- `cd elixir_wx_hand_pose_viewer` 後に `mix test` を実行し、初期状態の正常性を確認する
- `elixir ../scripts/setup_elixir_harness.exs elixir_wx_hand_pose_viewer` を実行して `mix qa` ハーネスを導入する（スクリプト未存在時は手動で `mix qa` 相当タスクを設定する）
- `mix qa` を実行し、成功を確認してから実装を開始する

3. 最小 GUI の作成
- `:wx` でウィンドウを開く最小構成を実装する
- アプリ起動・終了の基本フローを確認する

4. カメラ入力層の実装
- `Evision.VideoCapture` で USB カメラからフレーム取得する処理を実装する
- デバイス番号（例: 0）・解像度・FPS の初期値を定義し、取得失敗時にフォールバックする
- 取得失敗時のエラー表示と再試行方針を実装する（再試行間隔500ms開始、2倍指数バックオフ、上限8秒、最大10回）
- フレーム送信はキュー長上限を設け、古いフレームを破棄する
- フォールバック優先順を固定する（デバイス: `0 -> 1`、バックエンド: `V4L2 -> GStreamer`、解像度: `640x480 -> 1280x720 -> 320x240`）

5. 描画処理の実装
- 取得フレームを BGR から RGB へ変換し、`:wx` ウィンドウへ表示する
- 一定間隔で画面更新するループを実装する

6. 安定化対応
- フレーム取得と描画の負荷を見て更新間隔を調整する
- ウィンドウクローズ時にカメラリソースを確実に解放する
- カメラ取得プロセス停止と GUI 停止の終了順序を固定し、安全に停止する
- フレームのタイムスタンプを記録し、遅延を監視する
- ESCキー入力を捕捉して、ウィンドウクローズと同一の停止シーケンスを実行する
- 停止シーケンスは `GUI -> capture stop signal -> 2秒待機 -> VideoCapture.release -> wx destroy` を基本とする
- 停止待機2秒を超えた場合はキャプチャプロセスを終了させて強制終了する

7. 自動テストの先行導入（Phase 1）
- RGB変換の純関数テストを追加する
- フレーム破棄ポリシー（最新1フレーム保持）のテストを追加する
- 起動/停止シーケンスのプロセステストを追加する

8. 動作確認（Phase 1: カメラ表示基盤）
- カメラ未接続時の挙動を確認する
- カメラ接続時に映像が継続表示されることを確認する
- 長時間表示でクラッシュやメモリ増加がないか確認する
- `mix qa` の再実行を行う
- 手動GUI確認は `iex -S mix` で起動する
- 非対話実行確認は `mix run -e "..."` を使う（`--no-halt` は常駐検証時のみ）

9. 手認識機能の追加（Phase 2）
- 候補A（ONNX + Evision.dnn）を実装し、Pythonを使わずに手関節推定を行う
- 選定した推定器の出力座標を GUI 側に統合し、関節線を緑色で描画する
- 負荷計測を行い、FPS と解像度の既定値を調整する
- Phase 0で固定したモデル仕様・前後処理・しきい値に従って実装する

10. 仕上げ
- 実行手順を `README` に記載する
- 自動テスト方針を `README` に記載する
  - カメラ抽象層を Behaviour 化し、モックでフレーム供給テスト
  - RGB変換・座標描画の純関数テスト
  - 起動/停止シーケンスのプロセステスト
- コミットメッセージを日本語で作成し、初回コミットする
- モデルライセンス確認結果を `docs/model_licenses.md` に記録する

## フェーズ完了条件（Done）
- Phase 1 完了条件
  - カメラ表示、ウィンドウクローズ、ESC終了が動作する
  - `mix qa` が成功する
  - Phase 1対象の自動テスト（RGB変換、フレーム破棄、起動停止）が成功する
- Phase 2 完了条件
  - ONNX推論で関節点を重畳描画できる
  - 性能目標（平均FPS 15以上、P95遅延 200ms未満）を満たす
  - ライセンス記録が完了している
