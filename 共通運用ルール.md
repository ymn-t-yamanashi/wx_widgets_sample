# 共通運用ルール

このリポジトリ内のすべての計画・実装で、以下を共通ルールとする。

- コミットメッセージは必ず日本語で記述する。
- GUI実装は Elixir + `:wx`（wxWidgets）を基本構成とする。
- bashスクリプトの新規作成・運用を禁止し、共通処理はすべて Elixir（Mixタスク等）で実装する。
- 実行前に、開発は必ず Elixir で行う方針であることを確認する。
- 仕様どおりに完成するまで、サブエージェントを活用して実装と検証を反復する。
- 画面（スクリーン）実装はすべて Elixir で記述する。
- サブエージェントの反復実行は、各 Mix プロジェクトで共通利用できる Elixir タスク（例: `mix review_retry`）として実装・運用する。

## 実行前チェック（Elixir開発の標準手順）

以下を原則として上から順に実施する。

1. Elixir/Mix環境確認
- `elixir -v`
- `mix -v`

2. 依存関係の取得・更新
- `mix deps.get`

3. フォーマット適用（必須）
- `mix format`

4. 静的解析（可能なら実施）
- `mix credo`
- Credo未導入の場合は、導入検討または代替チェックを記録する。

5. コンパイル確認（警告も確認）
- `mix compile`
- 警告が出た場合は内容を確認し、必要に応じて修正する。

6. テスト実行
- `mix test`
- 失敗時は原因を特定し、修正後に再実行する。

7. 実行確認
- 対象アプリを `mix run` または `iex -S mix` で起動し、
  主要機能（画面起動、基本操作、エラー有無）を確認する。

## 追加でやるべきこと

- 変更内容に対応するテストを追加または更新する。
- READMEや計画書など、仕様変更に伴うドキュメント差分を更新する。
- 影響範囲（入力操作、描画、起動手順）を簡潔に記録する。
- コミット前に `git diff` を確認し、不要な差分がないことを確認する。

## ハーネス運用ルール

- 新規 Mix プロジェクト作成時は、必ず `mix qa` ハーネスを導入する。
- 導入コマンド: `elixir ./scripts/setup_elixir_harness.exs <mix_project_dir>`
- 導入後は対象プロジェクトで `mix qa` を実行し、成功を確認してから開発を進める。
- 可能な限り、コミット前にも `mix qa` を再実行して品質確認を行う。

## 追加ハーネス

### pre-commit ハーネス

- 導入: `elixir ./scripts/install_git_hooks.exs`
- 動作: コミット時に `sum_wx` で `mix qa` を自動実行する。
- 失敗時: コミットを中断する。

### release-smoke ハーネス

- 実行: `elixir ./scripts/release_smoke.exs`
- 動作: `mix qa` 実行後、DISPLAY がある環境では `mix run --no-halt` を5秒起動してGUI起動確認する。
- DISPLAY 未設定環境では GUI 起動確認をスキップし、手動確認コマンドを案内する。
